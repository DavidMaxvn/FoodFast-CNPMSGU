# Development Environment Configuration
# Override settings for local development

# Upload directory configuration for development
app.upload-dir=./uploads
app.images.dir=./uploads/products

# Database Configuration for development
spring.datasource.url=jdbc:mysql://localhost:3306/fastfood_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456

# Logging for development
logging.level.com.fastfood=DEBUG
logging.level.org.springframework.security=DEBUG